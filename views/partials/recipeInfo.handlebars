<div class="text-center">
  <h2>{{name}}</h2>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <div class="card-body">
        <p>{{ingredients}}</p>
  </div>
  <div class="card-body">
        <p>{{cooking_instructions}}</p>
  </div>
  <p>
    <button class="btn btn-primary">Leave a comment here</button>
  </p>
</div>